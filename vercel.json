{
  "installCommand": "if [ -d frontend ]; then cd frontend && npm ci; else npm ci; fi",
  "buildCommand": "if [ -d frontend ]; then cd frontend && npm run build; else npm run build && mkdir -p frontend && cp -R dist frontend/dist; fi",
  "outputDirectory": "frontend/dist",
  "rewrites": [
    {
      "source": "/((?!api/.*).*)",
      "destination": "/index.html"
    }
  ]
}
